# Configuration Base de Données MySQL
DB_NAME=cirque_app_dev
DB_USER=cirque_user
DB_PASSWORD=motdepassefort
DB_HOST=localhost
DB_PORT=3306

# Configuration Serveur
PORT=4000
NODE_ENV=development

# JWT Secret (générer avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=dev_jwt_secret_changez_moi_en_production

# CORS (Frontend URL)
FRONTEND_URL=http://localhost:3000

# Options de logging
LOG_LEVEL=debug

# Session
SESSION_SECRET=dev_session_secret_changez_moi_en_production
